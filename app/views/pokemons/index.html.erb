<h1>Pokemons</h1>

<% @pokemons.each do |pokemon| %>
  <p><%= "Pokemon: " %><%= link_to pokemon.name, pokemon_path(pokemon.id) %>&emsp;<%= "Type: " %><%= pokemon.pokemontype %></p>
<% end %>

<hr>

<%= link_to "[Add Pokemon]", new_pokemon_path %>
